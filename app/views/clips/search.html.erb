<% @result.each do |clip| %>
<li class="flex items-center gap-x-3.5 py-2 px-3 rounded-md text-sm text-gray-800 hover:bg-gray-200 focus:ring-2 focus:ring-gray-500" role="option" data-autocomplete-value="<%= clip.is_a?(Streamer) ? clip.streamer_name : clip.name %>" data-autocomplete-label="<%= clip.is_a?(Streamer) ? clip.streamer_name : clip.name %>">
  <%= clip.is_a?(Streamer) ? clip.streamer_name : clip.name %>
</li>
<% end %>